# TODO - SmartExpenseTracker Fixes

- [ ] Fix login to store correct userId (backend returns userId; frontend stores it)
- [ ] Remove hardcoded userId=1 from transaction creation/add transaction flows
- [ ] Ensure transaction request amount mapping to BigDecimal works (no null unless allowed)
- [ ] Test: register -> login -> profile loads correct user
- [ ] Test: add transaction -> dashboard shows it for same userId

